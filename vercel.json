{
  "version": 2,
  "buildCommand": "cd frontend && npm install && npx cross-env CI=false npm run build",
  "outputDirectory": "frontend/build",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
